# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production
.dev.vars

# raw Cloudinary export (migrated to R2, not committed)
photoscloudinary/

# player roster export (has emails/phone numbers, not committed)
players-export.csv

# raw headshot photos (migrated to R2, not committed)
solheadshots/

# raw personal photo/video archive (migrated to R2, not committed)
SOLTimeline/
scripts/generated-timeline-migration.sql

# one-time Cloudinary migration input (source of truth becomes D1 after migration)
scripts/cloudinary-albums.json
scripts/generated-migration.sql

# contacts export (has emails/phone/addresses/photos) and its parsed output
solcontacts.vcf
scripts/contacts_parsed.json
scripts/apply_contacts.sql

# wrangler local state
.wrangler/

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
